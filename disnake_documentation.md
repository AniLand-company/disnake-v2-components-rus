═══════════════════════════════════════════════════════════════════════════════
                ДИСНЕЙК v2 КОМПОНЕНТЫ (COMPONENTS V2)
                      ОБНОВЛЕНИЕ 2.11.0
═══════════════════════════════════════════════════════════════════════════════

📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ v2 КОМПОНЕНТОВ

Дата обновления: Сентябрь 2024
Версия: disnake 2.11.0+
Статус: ✅ Полностью поддерживается


═══════════════════════════════════════════════════════════════════════════════
📋 ОГЛАВЛЕНИЕ
═══════════════════════════════════════════════════════════════════════════════

1. ЧТО ТАКОЕ V2 КОМПОНЕНТЫ?
2. ОТЛИЧИЯ V2 ОТ V1
3. ОСНОВНЫЕ КОМПОНЕНТЫ V2
   - Section (Секция)
   - TextDisplay (Текстовое отображение)
   - Thumbnail (Миниатюра)
   - MediaGallery (Галерея медиа)
   - File (Файл)
   - Separator (Разделитель)
   - Container (Контейнер)
   - Label (Ярлык)
4. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
5. ОГРАНИЧЕНИЯ И ПРАВИЛА
6. МИГРАЦИЯ С V1 НА V2


═══════════════════════════════════════════════════════════════════════════════
1️⃣ ЧТО ТАКОЕ V2 КОМПОНЕНТЫ?
═══════════════════════════════════════════════════════════════════════════════

Определение:
─────────────────────────────────────────────────────────────────────────────
V2 компоненты - это совершенно новая система компонентов Discord, 
предназначенная для создания полностью управляемых компонентами сообщений.
Они заменяют классическую систему content + embeds на более гибкий подход.

Основные возможности v2:
✅ Полный контроль над макетом сообщения
✅ Используют только компоненты (нет content или embeds)
✅ Поддержка галерей, миниатюр, файлов
✅ Более естественное отображение информации
✅ Лучшая интеграция с современным UI Discord

Когда использовать v2?
─────────────────────────────────────────────────────────────────────────────
✅ ИСПОЛЬЗУЙ v2 КОГДА:
  - Нужен полный контроль над макетом
  - Отправляешь информацию, которая больше подходит для компонентов
  - Нужны галереи или медиа контент
  - Создаешь "красивые" информационные сообщения

❌ НЕ ИСПОЛЬЗУЙ v2 КОГДА:
  - Отправляешь просто текст (используй обычное сообщение)
  - Нужны все возможности embeds
  - Создаешь сложные интерактивные меню (View работает только с v1)


═══════════════════════════════════════════════════════════════════════════════
2️⃣ ОТЛИЧИЯ V2 ОТ V1
═══════════════════════════════════════════════════════════════════════════════

ТАБЛИЦА СРАВНЕНИЯ:
─────────────────────────────────────────────────────────────────────────────

┌─────────────────────────────────┬──────────────┬──────────────┐
│ Характеристика                  │     V1       │     V2       │
├─────────────────────────────────┼──────────────┼──────────────┤
│ Использует content/embeds       │     ✅       │      ❌      │
│ Кнопки и меню выбора            │     ✅       │      ❌      │
│ Интерактивные компоненты (View) │     ✅       │      ❌      │
│ Медиа галереи                   │     ❌       │      ✅      │
│ Текстовое отображение           │     ❌       │      ✅      │
│ Миниатюры/Thumbnail             │     ❌       │      ✅      │
│ Файлы в сообщении               │     ❌       │      ✅      │
│ Контейнеры                      │     ❌       │      ✅      │
│ Полный контроль макета          │      ✗       │      ✅      │
└─────────────────────────────────┴──────────────┴──────────────┘

ВАЖНО: V1 и V2 НЕ СОВМЕСТИМЫ!
Нельзя смешивать v1 компоненты (View, Button, Select меню) с v2 компонентами
в одном сообщении.


═══════════════════════════════════════════════════════════════════════════════
3️⃣ ОСНОВНЫЕ КОМПОНЕНТЫ V2
═══════════════════════════════════════════════════════════════════════════════

📌 SECTION (Секция)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент, который отображает текст рядом с аксессуаром (миниатюра или кнопка).
Похож на простой row с одной или тремя строками текста + изображение/кнопка.

Параметры:
- accessory: Аксессуар (Thumbnail или Button)
- id: Уникальный ID компонента (0-32 бит целое число)

Атрибуты:
- children: Список TextDisplay элементов (1-3)
- accessory: Thumbnail или Button
- id: Уникальный ID

Примечание: Для создания Section используй disnake.ui.Section, не Section из components.

Пример 1 - Section с текстом и миниатюрой:
```python
import disnake
from disnake.ext import commands

bot = commands.Bot(command_prefix="!")

@bot.command()
async def section_demo(ctx):
    # Создаем раздел с текстом и картинкой
    section = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content="**Красивый заголовок** 🎨"
        ),
        disnake.ui.TextDisplay(
            content="Вот это изображение справа →"
        ),
        accessory=disnake.ui.Thumbnail(
            media="https://example.com/image.png",
            description="Изображение для демо"
        )
    )
    
    await ctx.send(components=[section], flags=disnake.MessageFlags.is_components_v2)

bot.run("YOUR_TOKEN")
```

Пример 2 - Section с кнопкой:
```python
@bot.command()
async def section_button(ctx):
    section = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content="Нажми кнопку справа!"
        ),
        accessory=disnake.ui.Button(
            label="Кликни",
            style=disnake.ButtonStyle.primary,
            custom_id="demo_button"
        )
    )
    
    await ctx.send(components=[section], flags=disnake.MessageFlags.is_components_v2)
```


📌 TEXTDISPLAY (Текстовое отображение)
─────────────────────────────────────────────────────────────────────────────

Что это?
Простой компонент для отображения текста. Поддерживает markdown форматирование.

Параметры:
- content: Текст для отображения (ОБЯЗАТЕЛЕН)
- id: Уникальный ID

Атрибуты:
- content: Текст сообщения

Что поддерживает:
✅ **Жирный текст** (bold)
✅ *Курсив* (italic)
✅ __Подчеркнутый__ (underline)
✅ ~~Зачеркнутый~~ (strikethrough)
✅ `Код inline`
✅ ```Блоки кода```
✅ > Цитаты
✅ Списки
✅ Ссылки
✅ Эмодзи

Пример:
```python
@bot.command()
async def text_display(ctx):
    text = disnake.ui.TextDisplay(
        content="""
        # Заголовок
        
        Это **жирный** текст и *курсив*
        
        Можно использовать:
        • Списки
        • Маркеры
        • Эмодзи 🎉
        
        Даже `inline код` или 
        ```python
        блоки кода
        ```
        """
    )
    
    await ctx.send(components=[text], flags=disnake.MessageFlags.is_components_v2)
```


📌 THUMBNAIL (Миниатюра)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент для отображения изображения. Может использоваться как аксессуар 
в Section или отдельно.

Параметры:
- media: URL изображения или вложение (ОБЯЗАТЕЛЕН)
- description: Описание/альтекст изображения
- spoiler: Отмечать ли как спойлер (bool)
- id: Уникальный ID

Атрибуты:
- media: UnfurledMediaItem объект
- description: Описание изображения
- spoiler: Является ли спойлером

Источники media:
✅ Произвольный URL: "https://example.com/image.png"
✅ Вложение: "attachment://filename.png"
✅ Asset объект Discord

Пример 1 - Thumbnail как аксессуар:
```python
@bot.command()
async def thumbnail_accessory(ctx):
    section = disnake.ui.Section(
        disnake.ui.TextDisplay(content="Вот красивая картинка →"),
        accessory=disnake.ui.Thumbnail(
            media="https://i.imgur.com/AfFp7pu.png",
            description="Пример изображения"
        )
    )
    await ctx.send(components=[section], flags=disnake.MessageFlags.is_components_v2)
```

Пример 2 - Thumbnail как спойлер:
```python
@bot.command()
async def thumbnail_spoiler(ctx):
    thumb = disnake.ui.Thumbnail(
        media="https://example.com/image.png",
        description="Спойлер!",
        spoiler=True  # Будет скрыто спойлером
    )
    await ctx.send(components=[thumb], flags=disnake.MessageFlags.is_components_v2)
```


📌 MEDIAGALLERY (Галерея медиа)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент для отображения нескольких изображений в виде галереи/мозаики.
Максимум 10 изображений.

Параметры:
- items: Список MediaGalleryItem объектов (1-10)
- id: Уникальный ID

Класс MediaGalleryItem:
- media: Изображение (URL или вложение)
- description: Описание/альтекст (опционально)
- spoiler: Спойлер ли (опционально)

Пример - Галерея картинок:
```python
@bot.command()
async def gallery(ctx):
    gallery = disnake.ui.MediaGallery(
        disnake.ui.MediaGalleryItem(
            media="https://example.com/pic1.png",
            description="Первое изображение"
        ),
        disnake.ui.MediaGalleryItem(
            media="https://example.com/pic2.png",
            description="Второе изображение"
        ),
        disnake.ui.MediaGalleryItem(
            media="https://example.com/pic3.png",
            description="Третье изображение"
        ),
    )
    
    await ctx.send(components=[gallery], flags=disnake.MessageFlags.is_components_v2)
```

Пример 2 - Галерея со спойлерами:
```python
@bot.command()
async def spoiler_gallery(ctx):
    items = [
        disnake.ui.MediaGalleryItem(
            media=f"https://example.com/image{i}.png",
            spoiler=True
        )
        for i in range(1, 6)
    ]
    
    gallery = disnake.ui.MediaGallery(*items)
    await ctx.send(components=[gallery], flags=disnake.MessageFlags.is_components_v2)
```


📌 FILE (Файл)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент для отображения загруженного файла как вложения.

Параметры:
- file: UnfurledMediaItem с файлом (ОБЯЗАТЕЛЕН)
- spoiler: Спойлер ли (bool)
- name: Имя файла (опционально)
- size: Размер файла (опционально)
- id: Уникальный ID

Примечание: Используется для отображения документов, архивов и других 
не-медиа файлов.

Пример:
```python
@bot.command()
async def file_component(ctx):
    # Отправляем сообщение с файлом как компонентом
    file_comp = disnake.ui.File(
        file=disnake.ui.UnfurledMediaItem("attachment://document.pdf")
    )
    
    # Нужно передать реальный файл
    await ctx.send(
        components=[file_comp],
        files=[disnake.File("path/to/document.pdf")],
        flags=disnake.MessageFlags.is_components_v2
    )
```


📌 SEPARATOR (Разделитель)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент для создания вертикального разделителя или просто свободного места.
Позволяет добавлять пространство между компонентами.

Параметры:
- divider: Видимый ли разделитель (bool, по умолчанию True)
- spacing: Размер отступа (SeparatorSpacing enum)
- id: Уникальный ID

Размеры отступа (SeparatorSpacing):
- SMALL (0): Маленький отступ
- MEDIUM (1): Средний отступ (по умолчанию)
- LARGE (2): Большой отступ
- EXTRA_LARGE (3): Очень большой отступ

Пример 1 - Видимый разделитель:
```python
@bot.command()
async def separator_visible(ctx):
    section1 = disnake.ui.Section(
        disnake.ui.TextDisplay(content="Раздел 1")
    )
    
    separator = disnake.ui.Separator(divider=True)  # Видимая линия
    
    section2 = disnake.ui.Section(
        disnake.ui.TextDisplay(content="Раздел 2")
    )
    
    await ctx.send(
        components=[section1, separator, section2],
        flags=disnake.MessageFlags.is_components_v2
    )
```

Пример 2 - Просто пространство:
```python
@bot.command()
async def separator_spacing(ctx):
    text1 = disnake.ui.TextDisplay(content="Текст 1")
    
    # Просто пространство между элементами
    space = disnake.ui.Separator(
        divider=False,
        spacing=disnake.SeparatorSpacing.large
    )
    
    text2 = disnake.ui.TextDisplay(content="Текст 2")
    
    await ctx.send(
        components=[text1, space, text2],
        flags=disnake.MessageFlags.is_components_v2
    )
```


📌 CONTAINER (Контейнер)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент, который содержит другие компоненты. Похож на Embed, но для v2.
Создает визуально отделенный блок с опциональным цветом.

Параметры:
- components: Список компонентов внутри контейнера
- accent_colour/accent_color: Акцентный цвет (disnake.Color)
- spoiler: Спойлер ли весь контейнер (bool)
- id: Уникальный ID

Атрибуты:
- children: Список компонентов
- accent_colour: Цвет контейнера
- spoiler: Является ли спойлером

Пример 1 - Контейнер с цветом:
```python
@bot.command()
async def container(ctx):
    container = disnake.ui.Container(
        disnake.ui.TextDisplay(content="**Заголовок**"),
        disnake.ui.TextDisplay(content="Описание контейнера"),
        accent_colour=disnake.Color.blue()
    )
    
    await ctx.send(components=[container], flags=disnake.MessageFlags.is_components_v2)
```

Пример 2 - Вложенные контейнеры:
```python
@bot.command()
async def nested_containers(ctx):
    inner = disnake.ui.Container(
        disnake.ui.TextDisplay(content="Внутренний контейнер"),
        accent_colour=disnake.Color.green()
    )
    
    outer = disnake.ui.Container(
        disnake.ui.TextDisplay(content="Внешний контейнер"),
        inner,
        accent_colour=disnake.Color.blue()
    )
    
    await ctx.send(components=[outer], flags=disnake.MessageFlags.is_components_v2)
```


📌 LABEL (Ярлык)
─────────────────────────────────────────────────────────────────────────────

Что это?
Компонент для обертывания других компонентов с меткой и описанием.
Используется в основном в модальных окнах.

Параметры:
- label: Название метки (ОБЯЗАТЕЛЕН)
- component: Компонент который оборачивается (ОБЯЗАТЕЛЕН)
- description: Описание метки (опционально)

Пример:
```python
@bot.command()
async def label_example(ctx):
    # Label в основном используется в Modal
    # Но может быть использован и в других местах
    
    label = disnake.ui.Label(
        label="Имя пользователя",
        description="Введите ваше имя",
        component=disnake.ui.TextDisplay(
            content="Пример Label компонента"
        )
    )
```


═══════════════════════════════════════════════════════════════════════════════
4️⃣ ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
═══════════════════════════════════════════════════════════════════════════════

ПРИМЕР 1 - Профильная карточка
─────────────────────────────────────────────────────────────────────────────
```python
@bot.command()
async def profile_card(ctx, user: disnake.User = None):
    user = user or ctx.author
    
    # Создаем карточку профиля
    profile = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content=f"**{user.name}#{user.discriminator}**"
        ),
        disnake.ui.TextDisplay(
            content=f"ID: {user.id}\nСоздан: {user.created_at.strftime('%d.%m.%Y')}"
        ),
        accessory=disnake.ui.Thumbnail(
            media=user.avatar.url if user.avatar else user.default_avatar.url,
            description="Аватар пользователя"
        )
    )
    
    await ctx.send(
        components=[profile],
        flags=disnake.MessageFlags.is_components_v2
    )
```


ПРИМЕР 2 - Портфолио/Галерея
─────────────────────────────────────────────────────────────────────────────
```python
@bot.command()
async def portfolio(ctx):
    # Заголовок
    title = disnake.ui.Section(
        disnake.ui.TextDisplay(content="# 🎨 Мое Портфолио")
    )
    
    # Галерея работ
    gallery = disnake.ui.MediaGallery(
        disnake.ui.MediaGalleryItem(
            media="https://example.com/work1.png",
            description="Работа 1"
        ),
        disnake.ui.MediaGalleryItem(
            media="https://example.com/work2.png",
            description="Работа 2"
        ),
        disnake.ui.MediaGalleryItem(
            media="https://example.com/work3.png",
            description="Работа 3"
        ),
    )
    
    # Описание
    description = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content="Спасибо за просмотр моих работ! 🙏"
        )
    )
    
    await ctx.send(
        components=[title, gallery, description],
        flags=disnake.MessageFlags.is_components_v2
    )
```


ПРИМЕР 3 - Статья с иллюстрацией
─────────────────────────────────────────────────────────────────────────────
```python
@bot.command()
async def article(ctx):
    # Основная статья
    article_section = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content="""# Интересная Статья

Это начало нашей статьи с **жирным** текстом и другим 
форматированием.

• Пункт 1
• Пункт 2
• Пункт 3
            """
        ),
        disnake.ui.TextDisplay(
            content="Продолжение статьи со второй частью..."
        ),
        accessory=disnake.ui.Thumbnail(
            media="https://example.com/article-image.png",
            description="Иллюстрация к статье"
        )
    )
    
    # Разделитель
    separator = disnake.ui.Separator()
    
    # Контейнер с информацией об авторе
    author_info = disnake.ui.Container(
        disnake.ui.TextDisplay(content="### Об авторе"),
        disnake.ui.TextDisplay(
            content="Написано: Имя Автора\nДата: 15.09.2024"
        ),
        accent_colour=disnake.Color.gold()
    )
    
    await ctx.send(
        components=[article_section, separator, author_info],
        flags=disnake.MessageFlags.is_components_v2
    )
```


ПРИМЕР 4 - Анонс события
─────────────────────────────────────────────────────────────────────────────
```python
@bot.command()
async def event_announcement(ctx):
    # Заголовок события
    header = disnake.ui.Section(
        disnake.ui.TextDisplay(content="# 🎉 БОЛЬШОЕ СОБЫТИЕ!"),
        accessory=disnake.ui.Thumbnail(
            media="https://example.com/event-banner.png",
            description="Банер события"
        )
    )
    
    # Детали события
    details = disnake.ui.Container(
        disnake.ui.TextDisplay(
            content="""
            **📅 Дата:** 25 Сентября 2024
            **⏰ Время:** 19:00 МСК
            **📍 Место:** Discord сервер
            
            Не пропусти это событие!
            """
        ),
        accent_colour=disnake.Color.purple()
    )
    
    # Галерея спойлеров
    gallery = disnake.ui.MediaGallery(
        *[
            disnake.ui.MediaGalleryItem(
                media=f"https://example.com/preview{i}.png",
                spoiler=True
            )
            for i in range(1, 4)
        ]
    )
    
    # Финальное сообщение
    final = disnake.ui.Section(
        disnake.ui.TextDisplay(content="Приготовьтесь! 🚀")
    )
    
    await ctx.send(
        components=[header, details, gallery, final],
        flags=disnake.MessageFlags.is_components_v2
    )
```


═══════════════════════════════════════════════════════════════════════════════
5️⃣ ОГРАНИЧЕНИЯ И ПРАВИЛА
═══════════════════════════════════════════════════════════════════════════════

ВАЖНЫЕ ОГРАНИЧЕНИЯ:
─────────────────────────────────────────────────────────────────────────────

❌ НЕ СОВМЕСТИМО:
  • V2 компоненты с View (диспетчаризуемые компоненты v1)
  • V2 компоненты с content (текстовый контент сообщения)
  • V2 компоненты с embeds (встроенные сообщения)
  • V2 компоненты с poll (опросы)

✅ СОВМЕСТИМО:
  • V2 компоненты с файлами (files parameter)
  • V2 компоненты с allowed_mentions
  • V2 компоненты друг с другом

ВАЖНАЯ ФЛАГ:
─────────────────────────────────────────────────────────────────────────────
Когда отправляешь v2 компоненты, ОБЯЗАТЕЛЬНО установи флаг:

```python
flags=disnake.MessageFlags.is_components_v2
```

ИЛИ используй правильный способ:

```python
await ctx.send(
    components=[section, gallery],
    flags=disnake.MessageFlags.is_components_v2
)
```

НЮАНСЫ ФЛАГА:
  ⚠️ После установки флага его НЕЛЬЗЯ удалить
  ⚠️ Флаг устанавливается автоматически если используешь v2 компоненты
  ⚠️ Сообщение с флагом не может иметь content или embeds


ЛИМИТЫ КОМПОНЕНТОВ:
─────────────────────────────────────────────────────────────────────────────

Section:
  • Max: 3 TextDisplay компонента
  • 1 аксессуар (Thumbnail или Button)

MediaGallery:
  • Min: 1 изображение
  • Max: 10 изображений

TextDisplay:
  • Max 2000 символов контента
  • Поддерживает markdown

Container:
  • Может содержать другие компоненты
  • Max глубина вложений не документирована

ID компонентов:
  • От 1 до 2^32 - 1 (32-бит целое число)
  • 0 = автоматическое присваивание
  • Должны быть уникальны в пределах сообщения


═══════════════════════════════════════════════════════════════════════════════
6️⃣ МИГРАЦИЯ С V1 НА V2
═══════════════════════════════════════════════════════════════════════════════

КОГДА МИГРИРОВАТЬ?
─────────────────────────────────────────────────────────────────────────────

✅ ГОТОВО К V2:
  • Информационные сообщения
  • Профильные карточки
  • Портфолио и галереи
  • Объявления и новости
  • Статьи и гайды

❌ ОСТАЕТСЯ V1:
  • Интерактивные меню (View, Button, Select)
  • Формы обратной связи (Modal)
  • Систем голосования с кнопками
  • Любое реальное взаимодействие


КОД ДО И ПОСЛЕ:
─────────────────────────────────────────────────────────────────────────────

БЫЛО (V1):
```python
@bot.command()
async def info(ctx):
    embed = disnake.Embed(
        title="Информация",
        description="Это информационное сообщение",
        color=disnake.Color.blue()
    )
    embed.set_thumbnail(url="https://example.com/image.png")
    
    await ctx.send(embed=embed)
```

СТАЛО (V2):
```python
@bot.command()
async def info(ctx):
    # Заголовок
    title = disnake.ui.TextDisplay(
        content="## Информация"
    )
    
    # Описание с картинкой
    section = disnake.ui.Section(
        disnake.ui.TextDisplay(
            content="Это информационное сообщение"
        ),
        accessory=disnake.ui.Thumbnail(
            media="https://example.com/image.png"
        )
    )
    
    # Отправка с правильным флагом
    await ctx.send(
        components=[title, section],
        flags=disnake.MessageFlags.is_components_v2
    )
```


═══════════════════════════════════════════════════════════════════════════════
7️⃣ ПОЛЕЗНЫЕ ФУНКЦИИ
═══════════════════════════════════════════════════════════════════════════════

ui.walk_components()
─────────────────────────────────────────────────────────────────────────────
Функция для прохода по всем компонентам в сообщении.

```python
from disnake.ui import walk_components

message = await ctx.fetch_message(123456789)

for component in walk_components(message.components):
    print(f"Найден компонент: {type(component).__name__}")
```


ui.components_from_message()
─────────────────────────────────────────────────────────────────────────────
Получить все компоненты из сообщения в виде списка.

```python
from disnake.ui import components_from_message

message = await ctx.fetch_message(123456789)
components = components_from_message(message)

for comp in components:
    print(comp)
```


═══════════════════════════════════════════════════════════════════════════════
⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ
═══════════════════════════════════════════════════════════════════════════════

1️⃣ ВЛОЖЕНИЯ (ATTACHMENTS):
   Для использования вложений в компонентах используй формат:
   "attachment://filename.png"
   
   ВАЖНО: Файл должен быть передан через files parameter!

2️⃣ СПОЙЛЕРЫ:
   Компоненты поддерживают спойлеры через параметр spoiler=True
   Они будут скрыты пока пользователь не кликнет

3️⃣ ССЫЛКИ В ТЕКСТЕКСТЕ:
   V2 компоненты НЕ поддерживают встроенные ссылки как embeds
   Используй markdown синтаксис: [текст](url)

4️⃣ НЕЛЬЗЯ РЕДАКТИРОВАТЬ:
   Если сообщение использует v2 компоненты, его нельзя редактировать
   на сообщение без v2 (флаг необратим)

5️⃣ БОТЫ И ВЕБХУКИ:
   Обычные боты могут отправлять v2 компоненты
   Вебхуки тоже поддерживают v2 компоненты


═══════════════════════════════════════════════════════════════════════════════
📖 БЫСТРЫЙ СПРАВОЧНИК
═══════════════════════════════════════════════════════════════════════════════

СПИСОК ВСЕХ КОМПОНЕНТОВ V2:

✅ Section       → Текст + аксессуар (миниатюра/кнопка)
✅ TextDisplay   → Просто текст с markdown
✅ Thumbnail     → Одно изображение/картинка
✅ MediaGallery  → Галерея из 1-10 изображений
✅ File          → Файл/документ
✅ Separator     → Разделитель/пространство
✅ Container     → Контейнер с другими компонентами
✅ Label         → Метка для компонентов (основно в Modal)


КОД ШАБЛОН:

```python
# Базовый пример
component = disnake.ui.Section(
    disnake.ui.TextDisplay(content="Текст"),
    accessory=disnake.ui.Thumbnail(media="URL")
)

await ctx.send(
    components=[component],
    flags=disnake.MessageFlags.is_components_v2
)
```


═══════════════════════════════════════════════════════════════════════════════
📚 ССЫЛКИ НА РЕСУРСЫ
═══════════════════════════════════════════════════════════════════════════════

Официальная документация disnake:
  https://docs.disnake.dev/en/stable/

API Components v2:
  https://docs.disnake.dev/en/stable/api/components.html

UI Components v2:
  https://docs.disnake.dev/en/stable/api/ui.html

GitHub disnake:
  https://github.com/DisnakeDev/disnake

Changelog v2.11.0:
  https://docs.disnake.dev/en/stable/whats_new.html#v2-11-0


═══════════════════════════════════════════════════════════════════════════════
✅ ДОКУМЕНТАЦИЯ СОСТАВЛЕНА ПО ОФИЦИАЛЬНЫМ ИСТОЧНИКАМ
Disnake v2.11.0 - Сентябрь 2024
═══════════════════════════════════════════════════════════════════════════════
